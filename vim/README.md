# Vim Configuration

Originally based on https://github.com/JNRowe/vim-configs

## Installation

```shell
mv ~/.vim ~/.vim.bak
ln -s ~/dotfiles/vim ~/.vim
```

## Packages to install

- `wordnet` (for definitions)
